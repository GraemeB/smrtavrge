<%= form_for [@event,@forecast] do |f| %>
  <% if @forecast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forecast.errors.count, "error") %> prohibited this forecast from being saved:</h2>

      <ul>
      <% @forecast.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :forecast_result %><br />
    <%= f.number_field :forecast_result %>
  </div>
  <div class="field">
    <%= f.label :forecast_explination %><br />
    <%= f.text_area :forecast_explination %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
